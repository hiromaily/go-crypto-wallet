// Code generated by SQLBoiler 3.6.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// AccountKeyClient is an object representing the database table.
type AccountKeyClient struct {
	ID                    uint64    `boil:"id" json:"id" toml:"id" yaml:"id"`
	WalletAddress         string    `boil:"wallet_address" json:"wallet_address" toml:"wallet_address" yaml:"wallet_address"`
	P2SHSegwitAddress     string    `boil:"p2sh_segwit_address" json:"p2sh_segwit_address" toml:"p2sh_segwit_address" yaml:"p2sh_segwit_address"`
	FullPublicKey         string    `boil:"full_public_key" json:"full_public_key" toml:"full_public_key" yaml:"full_public_key"`
	WalletMultisigAddress string    `boil:"wallet_multisig_address" json:"wallet_multisig_address" toml:"wallet_multisig_address" yaml:"wallet_multisig_address"`
	RedeemScript          string    `boil:"redeem_script" json:"redeem_script" toml:"redeem_script" yaml:"redeem_script"`
	WalletImportFormat    string    `boil:"wallet_import_format" json:"wallet_import_format" toml:"wallet_import_format" yaml:"wallet_import_format"`
	Account               string    `boil:"account" json:"account" toml:"account" yaml:"account"`
	Idx                   uint64    `boil:"idx" json:"idx" toml:"idx" yaml:"idx"`
	KeyStatus             uint8     `boil:"key_status" json:"key_status" toml:"key_status" yaml:"key_status"`
	UpdatedAt             null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *accountKeyClientR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L accountKeyClientL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AccountKeyClientColumns = struct {
	ID                    string
	WalletAddress         string
	P2SHSegwitAddress     string
	FullPublicKey         string
	WalletMultisigAddress string
	RedeemScript          string
	WalletImportFormat    string
	Account               string
	Idx                   string
	KeyStatus             string
	UpdatedAt             string
}{
	ID:                    "id",
	WalletAddress:         "wallet_address",
	P2SHSegwitAddress:     "p2sh_segwit_address",
	FullPublicKey:         "full_public_key",
	WalletMultisigAddress: "wallet_multisig_address",
	RedeemScript:          "redeem_script",
	WalletImportFormat:    "wallet_import_format",
	Account:               "account",
	Idx:                   "idx",
	KeyStatus:             "key_status",
	UpdatedAt:             "updated_at",
}

// Generated where

var AccountKeyClientWhere = struct {
	ID                    whereHelperuint64
	WalletAddress         whereHelperstring
	P2SHSegwitAddress     whereHelperstring
	FullPublicKey         whereHelperstring
	WalletMultisigAddress whereHelperstring
	RedeemScript          whereHelperstring
	WalletImportFormat    whereHelperstring
	Account               whereHelperstring
	Idx                   whereHelperuint64
	KeyStatus             whereHelperuint8
	UpdatedAt             whereHelpernull_Time
}{
	ID:                    whereHelperuint64{field: "`account_key_client`.`id`"},
	WalletAddress:         whereHelperstring{field: "`account_key_client`.`wallet_address`"},
	P2SHSegwitAddress:     whereHelperstring{field: "`account_key_client`.`p2sh_segwit_address`"},
	FullPublicKey:         whereHelperstring{field: "`account_key_client`.`full_public_key`"},
	WalletMultisigAddress: whereHelperstring{field: "`account_key_client`.`wallet_multisig_address`"},
	RedeemScript:          whereHelperstring{field: "`account_key_client`.`redeem_script`"},
	WalletImportFormat:    whereHelperstring{field: "`account_key_client`.`wallet_import_format`"},
	Account:               whereHelperstring{field: "`account_key_client`.`account`"},
	Idx:                   whereHelperuint64{field: "`account_key_client`.`idx`"},
	KeyStatus:             whereHelperuint8{field: "`account_key_client`.`key_status`"},
	UpdatedAt:             whereHelpernull_Time{field: "`account_key_client`.`updated_at`"},
}

// AccountKeyClientRels is where relationship names are stored.
var AccountKeyClientRels = struct {
}{}

// accountKeyClientR is where relationships are stored.
type accountKeyClientR struct {
}

// NewStruct creates a new relationship struct
func (*accountKeyClientR) NewStruct() *accountKeyClientR {
	return &accountKeyClientR{}
}

// accountKeyClientL is where Load methods for each relationship are stored.
type accountKeyClientL struct{}

var (
	accountKeyClientAllColumns            = []string{"id", "wallet_address", "p2sh_segwit_address", "full_public_key", "wallet_multisig_address", "redeem_script", "wallet_import_format", "account", "idx", "key_status", "updated_at"}
	accountKeyClientColumnsWithoutDefault = []string{"wallet_address", "p2sh_segwit_address", "full_public_key", "wallet_multisig_address", "redeem_script", "wallet_import_format", "account", "idx"}
	accountKeyClientColumnsWithDefault    = []string{"id", "key_status", "updated_at"}
	accountKeyClientPrimaryKeyColumns     = []string{"id"}
)

type (
	// AccountKeyClientSlice is an alias for a slice of pointers to AccountKeyClient.
	// This should generally be used opposed to []AccountKeyClient.
	AccountKeyClientSlice []*AccountKeyClient

	accountKeyClientQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	accountKeyClientType                 = reflect.TypeOf(&AccountKeyClient{})
	accountKeyClientMapping              = queries.MakeStructMapping(accountKeyClientType)
	accountKeyClientPrimaryKeyMapping, _ = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, accountKeyClientPrimaryKeyColumns)
	accountKeyClientInsertCacheMut       sync.RWMutex
	accountKeyClientInsertCache          = make(map[string]insertCache)
	accountKeyClientUpdateCacheMut       sync.RWMutex
	accountKeyClientUpdateCache          = make(map[string]updateCache)
	accountKeyClientUpsertCacheMut       sync.RWMutex
	accountKeyClientUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single accountKeyClient record from the query.
func (q accountKeyClientQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AccountKeyClient, error) {
	o := &AccountKeyClient{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for account_key_client")
	}

	return o, nil
}

// All returns all AccountKeyClient records from the query.
func (q accountKeyClientQuery) All(ctx context.Context, exec boil.ContextExecutor) (AccountKeyClientSlice, error) {
	var o []*AccountKeyClient

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AccountKeyClient slice")
	}

	return o, nil
}

// Count returns the count of all AccountKeyClient records in the query.
func (q accountKeyClientQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count account_key_client rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q accountKeyClientQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if account_key_client exists")
	}

	return count > 0, nil
}

// AccountKeyClients retrieves all the records using an executor.
func AccountKeyClients(mods ...qm.QueryMod) accountKeyClientQuery {
	mods = append(mods, qm.From("`account_key_client`"))
	return accountKeyClientQuery{NewQuery(mods...)}
}

// FindAccountKeyClient retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAccountKeyClient(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*AccountKeyClient, error) {
	accountKeyClientObj := &AccountKeyClient{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `account_key_client` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, accountKeyClientObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from account_key_client")
	}

	return accountKeyClientObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AccountKeyClient) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no account_key_client provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(accountKeyClientColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	accountKeyClientInsertCacheMut.RLock()
	cache, cached := accountKeyClientInsertCache[key]
	accountKeyClientInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			accountKeyClientAllColumns,
			accountKeyClientColumnsWithDefault,
			accountKeyClientColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `account_key_client` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `account_key_client` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `account_key_client` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, accountKeyClientPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into account_key_client")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == accountKeyClientMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for account_key_client")
	}

CacheNoHooks:
	if !cached {
		accountKeyClientInsertCacheMut.Lock()
		accountKeyClientInsertCache[key] = cache
		accountKeyClientInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the AccountKeyClient.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AccountKeyClient) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	key := makeCacheKey(columns, nil)
	accountKeyClientUpdateCacheMut.RLock()
	cache, cached := accountKeyClientUpdateCache[key]
	accountKeyClientUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			accountKeyClientAllColumns,
			accountKeyClientPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update account_key_client, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `account_key_client` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, accountKeyClientPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, append(wl, accountKeyClientPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update account_key_client row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for account_key_client")
	}

	if !cached {
		accountKeyClientUpdateCacheMut.Lock()
		accountKeyClientUpdateCache[key] = cache
		accountKeyClientUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q accountKeyClientQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for account_key_client")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for account_key_client")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AccountKeyClientSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountKeyClientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `account_key_client` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountKeyClientPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in accountKeyClient slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all accountKeyClient")
	}
	return rowsAff, nil
}

var mySQLAccountKeyClientUniqueColumns = []string{
	"id",
	"wallet_address",
	"p2sh_segwit_address",
	"wallet_import_format",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AccountKeyClient) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no account_key_client provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	nzDefaults := queries.NonZeroDefaultSet(accountKeyClientColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLAccountKeyClientUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	accountKeyClientUpsertCacheMut.RLock()
	cache, cached := accountKeyClientUpsertCache[key]
	accountKeyClientUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			accountKeyClientAllColumns,
			accountKeyClientColumnsWithDefault,
			accountKeyClientColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			accountKeyClientAllColumns,
			accountKeyClientPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert account_key_client, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "account_key_client", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `account_key_client` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for account_key_client")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == accountKeyClientMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(accountKeyClientType, accountKeyClientMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for account_key_client")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for account_key_client")
	}

CacheNoHooks:
	if !cached {
		accountKeyClientUpsertCacheMut.Lock()
		accountKeyClientUpsertCache[key] = cache
		accountKeyClientUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single AccountKeyClient record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AccountKeyClient) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AccountKeyClient provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), accountKeyClientPrimaryKeyMapping)
	sql := "DELETE FROM `account_key_client` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from account_key_client")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for account_key_client")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q accountKeyClientQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no accountKeyClientQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from account_key_client")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for account_key_client")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AccountKeyClientSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountKeyClientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `account_key_client` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountKeyClientPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from accountKeyClient slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for account_key_client")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AccountKeyClient) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAccountKeyClient(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AccountKeyClientSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AccountKeyClientSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), accountKeyClientPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `account_key_client`.* FROM `account_key_client` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, accountKeyClientPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AccountKeyClientSlice")
	}

	*o = slice

	return nil
}

// AccountKeyClientExists checks if the AccountKeyClient row exists.
func AccountKeyClientExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `account_key_client` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if account_key_client exists")
	}

	return exists, nil
}
