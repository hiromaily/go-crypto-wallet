// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// BTCTXOutput is an object representing the database table.
type BTCTXOutput struct {
	ID            int64         `boil:"id" json:"id" toml:"id" yaml:"id"`
	TXID          int64         `boil:"tx_id" json:"tx_id" toml:"tx_id" yaml:"tx_id"`
	OutputAddress string        `boil:"output_address" json:"output_address" toml:"output_address" yaml:"output_address"`
	OutputAccount string        `boil:"output_account" json:"output_account" toml:"output_account" yaml:"output_account"`
	OutputAmount  types.Decimal `boil:"output_amount" json:"output_amount" toml:"output_amount" yaml:"output_amount"`
	IsChange      bool          `boil:"is_change" json:"is_change" toml:"is_change" yaml:"is_change"`
	UpdatedAt     null.Time     `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *btcTXOutputR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L btcTXOutputL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BTCTXOutputColumns = struct {
	ID            string
	TXID          string
	OutputAddress string
	OutputAccount string
	OutputAmount  string
	IsChange      string
	UpdatedAt     string
}{
	ID:            "id",
	TXID:          "tx_id",
	OutputAddress: "output_address",
	OutputAccount: "output_account",
	OutputAmount:  "output_amount",
	IsChange:      "is_change",
	UpdatedAt:     "updated_at",
}

// Generated where

var BTCTXOutputWhere = struct {
	ID            whereHelperint64
	TXID          whereHelperint64
	OutputAddress whereHelperstring
	OutputAccount whereHelperstring
	OutputAmount  whereHelpertypes_Decimal
	IsChange      whereHelperbool
	UpdatedAt     whereHelpernull_Time
}{
	ID:            whereHelperint64{field: "`btc_tx_output`.`id`"},
	TXID:          whereHelperint64{field: "`btc_tx_output`.`tx_id`"},
	OutputAddress: whereHelperstring{field: "`btc_tx_output`.`output_address`"},
	OutputAccount: whereHelperstring{field: "`btc_tx_output`.`output_account`"},
	OutputAmount:  whereHelpertypes_Decimal{field: "`btc_tx_output`.`output_amount`"},
	IsChange:      whereHelperbool{field: "`btc_tx_output`.`is_change`"},
	UpdatedAt:     whereHelpernull_Time{field: "`btc_tx_output`.`updated_at`"},
}

// BTCTXOutputRels is where relationship names are stored.
var BTCTXOutputRels = struct {
}{}

// btcTXOutputR is where relationships are stored.
type btcTXOutputR struct {
}

// NewStruct creates a new relationship struct
func (*btcTXOutputR) NewStruct() *btcTXOutputR {
	return &btcTXOutputR{}
}

// btcTXOutputL is where Load methods for each relationship are stored.
type btcTXOutputL struct{}

var (
	btcTXOutputAllColumns            = []string{"id", "tx_id", "output_address", "output_account", "output_amount", "is_change", "updated_at"}
	btcTXOutputColumnsWithoutDefault = []string{"tx_id", "output_address", "output_account", "output_amount"}
	btcTXOutputColumnsWithDefault    = []string{"id", "is_change", "updated_at"}
	btcTXOutputPrimaryKeyColumns     = []string{"id"}
)

type (
	// BTCTXOutputSlice is an alias for a slice of pointers to BTCTXOutput.
	// This should generally be used opposed to []BTCTXOutput.
	BTCTXOutputSlice []*BTCTXOutput

	btcTXOutputQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	btcTXOutputType                 = reflect.TypeOf(&BTCTXOutput{})
	btcTXOutputMapping              = queries.MakeStructMapping(btcTXOutputType)
	btcTXOutputPrimaryKeyMapping, _ = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, btcTXOutputPrimaryKeyColumns)
	btcTXOutputInsertCacheMut       sync.RWMutex
	btcTXOutputInsertCache          = make(map[string]insertCache)
	btcTXOutputUpdateCacheMut       sync.RWMutex
	btcTXOutputUpdateCache          = make(map[string]updateCache)
	btcTXOutputUpsertCacheMut       sync.RWMutex
	btcTXOutputUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single btcTXOutput record from the query.
func (q btcTXOutputQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BTCTXOutput, error) {
	o := &BTCTXOutput{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for btc_tx_output")
	}

	return o, nil
}

// All returns all BTCTXOutput records from the query.
func (q btcTXOutputQuery) All(ctx context.Context, exec boil.ContextExecutor) (BTCTXOutputSlice, error) {
	var o []*BTCTXOutput

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BTCTXOutput slice")
	}

	return o, nil
}

// Count returns the count of all BTCTXOutput records in the query.
func (q btcTXOutputQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count btc_tx_output rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q btcTXOutputQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if btc_tx_output exists")
	}

	return count > 0, nil
}

// BTCTXOutputs retrieves all the records using an executor.
func BTCTXOutputs(mods ...qm.QueryMod) btcTXOutputQuery {
	mods = append(mods, qm.From("`btc_tx_output`"))
	return btcTXOutputQuery{NewQuery(mods...)}
}

// FindBTCTXOutput retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBTCTXOutput(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*BTCTXOutput, error) {
	btcTXOutputObj := &BTCTXOutput{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `btc_tx_output` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, btcTXOutputObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from btc_tx_output")
	}

	return btcTXOutputObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BTCTXOutput) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no btc_tx_output provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(btcTXOutputColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	btcTXOutputInsertCacheMut.RLock()
	cache, cached := btcTXOutputInsertCache[key]
	btcTXOutputInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			btcTXOutputAllColumns,
			btcTXOutputColumnsWithDefault,
			btcTXOutputColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `btc_tx_output` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `btc_tx_output` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `btc_tx_output` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, btcTXOutputPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into btc_tx_output")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == btcTXOutputMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for btc_tx_output")
	}

CacheNoHooks:
	if !cached {
		btcTXOutputInsertCacheMut.Lock()
		btcTXOutputInsertCache[key] = cache
		btcTXOutputInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the BTCTXOutput.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BTCTXOutput) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	key := makeCacheKey(columns, nil)
	btcTXOutputUpdateCacheMut.RLock()
	cache, cached := btcTXOutputUpdateCache[key]
	btcTXOutputUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			btcTXOutputAllColumns,
			btcTXOutputPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update btc_tx_output, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `btc_tx_output` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, btcTXOutputPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, append(wl, btcTXOutputPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update btc_tx_output row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for btc_tx_output")
	}

	if !cached {
		btcTXOutputUpdateCacheMut.Lock()
		btcTXOutputUpdateCache[key] = cache
		btcTXOutputUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q btcTXOutputQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for btc_tx_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for btc_tx_output")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BTCTXOutputSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), btcTXOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `btc_tx_output` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, btcTXOutputPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in btcTXOutput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all btcTXOutput")
	}
	return rowsAff, nil
}

var mySQLBTCTXOutputUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BTCTXOutput) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no btc_tx_output provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	nzDefaults := queries.NonZeroDefaultSet(btcTXOutputColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLBTCTXOutputUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	btcTXOutputUpsertCacheMut.RLock()
	cache, cached := btcTXOutputUpsertCache[key]
	btcTXOutputUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			btcTXOutputAllColumns,
			btcTXOutputColumnsWithDefault,
			btcTXOutputColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			btcTXOutputAllColumns,
			btcTXOutputPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert btc_tx_output, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`btc_tx_output`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `btc_tx_output` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for btc_tx_output")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == btcTXOutputMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(btcTXOutputType, btcTXOutputMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for btc_tx_output")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for btc_tx_output")
	}

CacheNoHooks:
	if !cached {
		btcTXOutputUpsertCacheMut.Lock()
		btcTXOutputUpsertCache[key] = cache
		btcTXOutputUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single BTCTXOutput record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BTCTXOutput) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BTCTXOutput provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), btcTXOutputPrimaryKeyMapping)
	sql := "DELETE FROM `btc_tx_output` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from btc_tx_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for btc_tx_output")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q btcTXOutputQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no btcTXOutputQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from btc_tx_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for btc_tx_output")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BTCTXOutputSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), btcTXOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `btc_tx_output` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, btcTXOutputPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from btcTXOutput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for btc_tx_output")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BTCTXOutput) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBTCTXOutput(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BTCTXOutputSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BTCTXOutputSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), btcTXOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `btc_tx_output`.* FROM `btc_tx_output` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, btcTXOutputPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BTCTXOutputSlice")
	}

	*o = slice

	return nil
}

// BTCTXOutputExists checks if the BTCTXOutput row exists.
func BTCTXOutputExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `btc_tx_output` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if btc_tx_output exists")
	}

	return exists, nil
}

// InsertAll inserts all rows with the specified column values, using an executor.
func (o BTCTXOutputSlice) InsertAll(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	var sql string
	vals := []interface{}{}
	for i, row := range o {
		if !boil.TimestampsAreSkipped(ctx) {
			currTime := time.Now().In(boil.GetLocation())

			if queries.MustTime(row.UpdatedAt).IsZero() {
				queries.SetScanner(&row.UpdatedAt, currTime)
			}
		}

		nzDefaults := queries.NonZeroDefaultSet(btcTXOutputColumnsWithDefault, row)
		wl, _ := columns.InsertColumnSet(
			btcTXOutputAllColumns,
			btcTXOutputColumnsWithDefault,
			btcTXOutputColumnsWithoutDefault,
			nzDefaults,
		)
		if i == 0 {
			sql = "INSERT INTO `btc_tx_output` " + "(`" + strings.Join(wl, "`,`") + "`)" + " VALUES "
		}
		sql += strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), len(vals)+1, len(wl))
		if i != len(o)-1 {
			sql += ","
		}
		valMapping, err := queries.BindMapping(btcTXOutputType, btcTXOutputMapping, wl)
		if err != nil {
			return err
		}
		value := reflect.Indirect(reflect.ValueOf(row))
		vals = append(vals, queries.ValuesFromMapping(value, valMapping)...)
	}
	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, vals...)
	}

	_, err := exec.ExecContext(ctx, sql, vals...)
	if err != nil {
		return errors.Wrap(err, "models: unable to insert into btc_tx_output")
	}

	return nil
}
