// Code generated by SQLBoiler 3.6.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// TXTransferInput is an object representing the database table.
type TXTransferInput struct {
	ID                 uint64        `boil:"id" json:"id" toml:"id" yaml:"id"`
	ReceiptID          uint64        `boil:"receipt_id" json:"receipt_id" toml:"receipt_id" yaml:"receipt_id"`
	InputTxid          string        `boil:"input_txid" json:"input_txid" toml:"input_txid" yaml:"input_txid"`
	InputVout          int           `boil:"input_vout" json:"input_vout" toml:"input_vout" yaml:"input_vout"`
	InputAddress       string        `boil:"input_address" json:"input_address" toml:"input_address" yaml:"input_address"`
	InputAccount       string        `boil:"input_account" json:"input_account" toml:"input_account" yaml:"input_account"`
	InputAmount        types.Decimal `boil:"input_amount" json:"input_amount" toml:"input_amount" yaml:"input_amount"`
	InputConfirmations uint64        `boil:"input_confirmations" json:"input_confirmations" toml:"input_confirmations" yaml:"input_confirmations"`
	UpdatedAt          null.Time     `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *txTransferInputR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L txTransferInputL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TXTransferInputColumns = struct {
	ID                 string
	ReceiptID          string
	InputTxid          string
	InputVout          string
	InputAddress       string
	InputAccount       string
	InputAmount        string
	InputConfirmations string
	UpdatedAt          string
}{
	ID:                 "id",
	ReceiptID:          "receipt_id",
	InputTxid:          "input_txid",
	InputVout:          "input_vout",
	InputAddress:       "input_address",
	InputAccount:       "input_account",
	InputAmount:        "input_amount",
	InputConfirmations: "input_confirmations",
	UpdatedAt:          "updated_at",
}

// Generated where

var TXTransferInputWhere = struct {
	ID                 whereHelperuint64
	ReceiptID          whereHelperuint64
	InputTxid          whereHelperstring
	InputVout          whereHelperint
	InputAddress       whereHelperstring
	InputAccount       whereHelperstring
	InputAmount        whereHelpertypes_Decimal
	InputConfirmations whereHelperuint64
	UpdatedAt          whereHelpernull_Time
}{
	ID:                 whereHelperuint64{field: "`tx_transfer_input`.`id`"},
	ReceiptID:          whereHelperuint64{field: "`tx_transfer_input`.`receipt_id`"},
	InputTxid:          whereHelperstring{field: "`tx_transfer_input`.`input_txid`"},
	InputVout:          whereHelperint{field: "`tx_transfer_input`.`input_vout`"},
	InputAddress:       whereHelperstring{field: "`tx_transfer_input`.`input_address`"},
	InputAccount:       whereHelperstring{field: "`tx_transfer_input`.`input_account`"},
	InputAmount:        whereHelpertypes_Decimal{field: "`tx_transfer_input`.`input_amount`"},
	InputConfirmations: whereHelperuint64{field: "`tx_transfer_input`.`input_confirmations`"},
	UpdatedAt:          whereHelpernull_Time{field: "`tx_transfer_input`.`updated_at`"},
}

// TXTransferInputRels is where relationship names are stored.
var TXTransferInputRels = struct {
}{}

// txTransferInputR is where relationships are stored.
type txTransferInputR struct {
}

// NewStruct creates a new relationship struct
func (*txTransferInputR) NewStruct() *txTransferInputR {
	return &txTransferInputR{}
}

// txTransferInputL is where Load methods for each relationship are stored.
type txTransferInputL struct{}

var (
	txTransferInputAllColumns            = []string{"id", "receipt_id", "input_txid", "input_vout", "input_address", "input_account", "input_amount", "input_confirmations", "updated_at"}
	txTransferInputColumnsWithoutDefault = []string{"receipt_id", "input_txid", "input_vout", "input_address", "input_account", "input_amount", "input_confirmations"}
	txTransferInputColumnsWithDefault    = []string{"id", "updated_at"}
	txTransferInputPrimaryKeyColumns     = []string{"id"}
)

type (
	// TXTransferInputSlice is an alias for a slice of pointers to TXTransferInput.
	// This should generally be used opposed to []TXTransferInput.
	TXTransferInputSlice []*TXTransferInput

	txTransferInputQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	txTransferInputType                 = reflect.TypeOf(&TXTransferInput{})
	txTransferInputMapping              = queries.MakeStructMapping(txTransferInputType)
	txTransferInputPrimaryKeyMapping, _ = queries.BindMapping(txTransferInputType, txTransferInputMapping, txTransferInputPrimaryKeyColumns)
	txTransferInputInsertCacheMut       sync.RWMutex
	txTransferInputInsertCache          = make(map[string]insertCache)
	txTransferInputUpdateCacheMut       sync.RWMutex
	txTransferInputUpdateCache          = make(map[string]updateCache)
	txTransferInputUpsertCacheMut       sync.RWMutex
	txTransferInputUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single txTransferInput record from the query.
func (q txTransferInputQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TXTransferInput, error) {
	o := &TXTransferInput{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tx_transfer_input")
	}

	return o, nil
}

// All returns all TXTransferInput records from the query.
func (q txTransferInputQuery) All(ctx context.Context, exec boil.ContextExecutor) (TXTransferInputSlice, error) {
	var o []*TXTransferInput

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TXTransferInput slice")
	}

	return o, nil
}

// Count returns the count of all TXTransferInput records in the query.
func (q txTransferInputQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tx_transfer_input rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q txTransferInputQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tx_transfer_input exists")
	}

	return count > 0, nil
}

// TXTransferInputs retrieves all the records using an executor.
func TXTransferInputs(mods ...qm.QueryMod) txTransferInputQuery {
	mods = append(mods, qm.From("`tx_transfer_input`"))
	return txTransferInputQuery{NewQuery(mods...)}
}

// FindTXTransferInput retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTXTransferInput(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*TXTransferInput, error) {
	txTransferInputObj := &TXTransferInput{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tx_transfer_input` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, txTransferInputObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tx_transfer_input")
	}

	return txTransferInputObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TXTransferInput) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tx_transfer_input provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(txTransferInputColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	txTransferInputInsertCacheMut.RLock()
	cache, cached := txTransferInputInsertCache[key]
	txTransferInputInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			txTransferInputAllColumns,
			txTransferInputColumnsWithDefault,
			txTransferInputColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tx_transfer_input` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tx_transfer_input` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tx_transfer_input` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, txTransferInputPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tx_transfer_input")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == txTransferInputMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tx_transfer_input")
	}

CacheNoHooks:
	if !cached {
		txTransferInputInsertCacheMut.Lock()
		txTransferInputInsertCache[key] = cache
		txTransferInputInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the TXTransferInput.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TXTransferInput) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	key := makeCacheKey(columns, nil)
	txTransferInputUpdateCacheMut.RLock()
	cache, cached := txTransferInputUpdateCache[key]
	txTransferInputUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			txTransferInputAllColumns,
			txTransferInputPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tx_transfer_input, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tx_transfer_input` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, txTransferInputPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, append(wl, txTransferInputPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tx_transfer_input row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tx_transfer_input")
	}

	if !cached {
		txTransferInputUpdateCacheMut.Lock()
		txTransferInputUpdateCache[key] = cache
		txTransferInputUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q txTransferInputQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tx_transfer_input")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tx_transfer_input")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TXTransferInputSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferInputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tx_transfer_input` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferInputPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in txTransferInput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all txTransferInput")
	}
	return rowsAff, nil
}

var mySQLTXTransferInputUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TXTransferInput) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tx_transfer_input provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	nzDefaults := queries.NonZeroDefaultSet(txTransferInputColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTXTransferInputUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	txTransferInputUpsertCacheMut.RLock()
	cache, cached := txTransferInputUpsertCache[key]
	txTransferInputUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			txTransferInputAllColumns,
			txTransferInputColumnsWithDefault,
			txTransferInputColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			txTransferInputAllColumns,
			txTransferInputPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert tx_transfer_input, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "tx_transfer_input", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tx_transfer_input` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tx_transfer_input")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == txTransferInputMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(txTransferInputType, txTransferInputMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tx_transfer_input")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tx_transfer_input")
	}

CacheNoHooks:
	if !cached {
		txTransferInputUpsertCacheMut.Lock()
		txTransferInputUpsertCache[key] = cache
		txTransferInputUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single TXTransferInput record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TXTransferInput) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TXTransferInput provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), txTransferInputPrimaryKeyMapping)
	sql := "DELETE FROM `tx_transfer_input` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tx_transfer_input")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tx_transfer_input")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q txTransferInputQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no txTransferInputQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tx_transfer_input")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tx_transfer_input")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TXTransferInputSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferInputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tx_transfer_input` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferInputPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from txTransferInput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tx_transfer_input")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TXTransferInput) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTXTransferInput(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TXTransferInputSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TXTransferInputSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferInputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tx_transfer_input`.* FROM `tx_transfer_input` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferInputPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TXTransferInputSlice")
	}

	*o = slice

	return nil
}

// TXTransferInputExists checks if the TXTransferInput row exists.
func TXTransferInputExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tx_transfer_input` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tx_transfer_input exists")
	}

	return exists, nil
}
