// Code generated by SQLBoiler 3.6.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// TXTransferOutput is an object representing the database table.
type TXTransferOutput struct {
	ID            uint64        `boil:"id" json:"id" toml:"id" yaml:"id"`
	ReceiptID     uint64        `boil:"receipt_id" json:"receipt_id" toml:"receipt_id" yaml:"receipt_id"`
	OutputAddress string        `boil:"output_address" json:"output_address" toml:"output_address" yaml:"output_address"`
	OutputAccount string        `boil:"output_account" json:"output_account" toml:"output_account" yaml:"output_account"`
	OutputAmount  types.Decimal `boil:"output_amount" json:"output_amount" toml:"output_amount" yaml:"output_amount"`
	IsChange      null.Bool     `boil:"is_change" json:"is_change,omitempty" toml:"is_change" yaml:"is_change,omitempty"`
	UpdatedAt     null.Time     `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *txTransferOutputR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L txTransferOutputL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TXTransferOutputColumns = struct {
	ID            string
	ReceiptID     string
	OutputAddress string
	OutputAccount string
	OutputAmount  string
	IsChange      string
	UpdatedAt     string
}{
	ID:            "id",
	ReceiptID:     "receipt_id",
	OutputAddress: "output_address",
	OutputAccount: "output_account",
	OutputAmount:  "output_amount",
	IsChange:      "is_change",
	UpdatedAt:     "updated_at",
}

// Generated where

var TXTransferOutputWhere = struct {
	ID            whereHelperuint64
	ReceiptID     whereHelperuint64
	OutputAddress whereHelperstring
	OutputAccount whereHelperstring
	OutputAmount  whereHelpertypes_Decimal
	IsChange      whereHelpernull_Bool
	UpdatedAt     whereHelpernull_Time
}{
	ID:            whereHelperuint64{field: "`tx_transfer_output`.`id`"},
	ReceiptID:     whereHelperuint64{field: "`tx_transfer_output`.`receipt_id`"},
	OutputAddress: whereHelperstring{field: "`tx_transfer_output`.`output_address`"},
	OutputAccount: whereHelperstring{field: "`tx_transfer_output`.`output_account`"},
	OutputAmount:  whereHelpertypes_Decimal{field: "`tx_transfer_output`.`output_amount`"},
	IsChange:      whereHelpernull_Bool{field: "`tx_transfer_output`.`is_change`"},
	UpdatedAt:     whereHelpernull_Time{field: "`tx_transfer_output`.`updated_at`"},
}

// TXTransferOutputRels is where relationship names are stored.
var TXTransferOutputRels = struct {
}{}

// txTransferOutputR is where relationships are stored.
type txTransferOutputR struct {
}

// NewStruct creates a new relationship struct
func (*txTransferOutputR) NewStruct() *txTransferOutputR {
	return &txTransferOutputR{}
}

// txTransferOutputL is where Load methods for each relationship are stored.
type txTransferOutputL struct{}

var (
	txTransferOutputAllColumns            = []string{"id", "receipt_id", "output_address", "output_account", "output_amount", "is_change", "updated_at"}
	txTransferOutputColumnsWithoutDefault = []string{"receipt_id", "output_address", "output_account", "output_amount"}
	txTransferOutputColumnsWithDefault    = []string{"id", "is_change", "updated_at"}
	txTransferOutputPrimaryKeyColumns     = []string{"id"}
)

type (
	// TXTransferOutputSlice is an alias for a slice of pointers to TXTransferOutput.
	// This should generally be used opposed to []TXTransferOutput.
	TXTransferOutputSlice []*TXTransferOutput

	txTransferOutputQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	txTransferOutputType                 = reflect.TypeOf(&TXTransferOutput{})
	txTransferOutputMapping              = queries.MakeStructMapping(txTransferOutputType)
	txTransferOutputPrimaryKeyMapping, _ = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, txTransferOutputPrimaryKeyColumns)
	txTransferOutputInsertCacheMut       sync.RWMutex
	txTransferOutputInsertCache          = make(map[string]insertCache)
	txTransferOutputUpdateCacheMut       sync.RWMutex
	txTransferOutputUpdateCache          = make(map[string]updateCache)
	txTransferOutputUpsertCacheMut       sync.RWMutex
	txTransferOutputUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single txTransferOutput record from the query.
func (q txTransferOutputQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TXTransferOutput, error) {
	o := &TXTransferOutput{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for tx_transfer_output")
	}

	return o, nil
}

// All returns all TXTransferOutput records from the query.
func (q txTransferOutputQuery) All(ctx context.Context, exec boil.ContextExecutor) (TXTransferOutputSlice, error) {
	var o []*TXTransferOutput

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TXTransferOutput slice")
	}

	return o, nil
}

// Count returns the count of all TXTransferOutput records in the query.
func (q txTransferOutputQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count tx_transfer_output rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q txTransferOutputQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if tx_transfer_output exists")
	}

	return count > 0, nil
}

// TXTransferOutputs retrieves all the records using an executor.
func TXTransferOutputs(mods ...qm.QueryMod) txTransferOutputQuery {
	mods = append(mods, qm.From("`tx_transfer_output`"))
	return txTransferOutputQuery{NewQuery(mods...)}
}

// FindTXTransferOutput retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTXTransferOutput(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*TXTransferOutput, error) {
	txTransferOutputObj := &TXTransferOutput{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `tx_transfer_output` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, txTransferOutputObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from tx_transfer_output")
	}

	return txTransferOutputObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TXTransferOutput) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tx_transfer_output provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(txTransferOutputColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	txTransferOutputInsertCacheMut.RLock()
	cache, cached := txTransferOutputInsertCache[key]
	txTransferOutputInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			txTransferOutputAllColumns,
			txTransferOutputColumnsWithDefault,
			txTransferOutputColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `tx_transfer_output` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `tx_transfer_output` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `tx_transfer_output` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, txTransferOutputPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into tx_transfer_output")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == txTransferOutputMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tx_transfer_output")
	}

CacheNoHooks:
	if !cached {
		txTransferOutputInsertCacheMut.Lock()
		txTransferOutputInsertCache[key] = cache
		txTransferOutputInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the TXTransferOutput.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TXTransferOutput) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	key := makeCacheKey(columns, nil)
	txTransferOutputUpdateCacheMut.RLock()
	cache, cached := txTransferOutputUpdateCache[key]
	txTransferOutputUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			txTransferOutputAllColumns,
			txTransferOutputPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update tx_transfer_output, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `tx_transfer_output` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, txTransferOutputPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, append(wl, txTransferOutputPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update tx_transfer_output row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for tx_transfer_output")
	}

	if !cached {
		txTransferOutputUpdateCacheMut.Lock()
		txTransferOutputUpdateCache[key] = cache
		txTransferOutputUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q txTransferOutputQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for tx_transfer_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for tx_transfer_output")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TXTransferOutputSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `tx_transfer_output` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferOutputPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in txTransferOutput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all txTransferOutput")
	}
	return rowsAff, nil
}

var mySQLTXTransferOutputUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TXTransferOutput) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no tx_transfer_output provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	nzDefaults := queries.NonZeroDefaultSet(txTransferOutputColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTXTransferOutputUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	txTransferOutputUpsertCacheMut.RLock()
	cache, cached := txTransferOutputUpsertCache[key]
	txTransferOutputUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			txTransferOutputAllColumns,
			txTransferOutputColumnsWithDefault,
			txTransferOutputColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			txTransferOutputAllColumns,
			txTransferOutputPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert tx_transfer_output, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "tx_transfer_output", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `tx_transfer_output` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for tx_transfer_output")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == txTransferOutputMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(txTransferOutputType, txTransferOutputMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for tx_transfer_output")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for tx_transfer_output")
	}

CacheNoHooks:
	if !cached {
		txTransferOutputUpsertCacheMut.Lock()
		txTransferOutputUpsertCache[key] = cache
		txTransferOutputUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single TXTransferOutput record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TXTransferOutput) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TXTransferOutput provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), txTransferOutputPrimaryKeyMapping)
	sql := "DELETE FROM `tx_transfer_output` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from tx_transfer_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for tx_transfer_output")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q txTransferOutputQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no txTransferOutputQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tx_transfer_output")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tx_transfer_output")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TXTransferOutputSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `tx_transfer_output` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferOutputPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from txTransferOutput slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for tx_transfer_output")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TXTransferOutput) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTXTransferOutput(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TXTransferOutputSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TXTransferOutputSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txTransferOutputPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `tx_transfer_output`.* FROM `tx_transfer_output` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, txTransferOutputPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TXTransferOutputSlice")
	}

	*o = slice

	return nil
}

// TXTransferOutputExists checks if the TXTransferOutput row exists.
func TXTransferOutputExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `tx_transfer_output` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if tx_transfer_output exists")
	}

	return exists, nil
}
