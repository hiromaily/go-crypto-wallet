-- Code generated by `make extract-sqlc-schema-postgres-keygen`. DO NOT EDIT.
-- Extracted from dump_keygen.postgres.sql

CREATE TABLE auth_fullpubkey (
    id smallint NOT NULL,
    coin coin_btc_bch NOT NULL,
    auth_account character varying(20) NOT NULL,
    purpose smallint DEFAULT 49 NOT NULL,
    full_public_key character varying(255) NOT NULL,
    extended_pubkey character varying(255),
    fingerprint character varying(8),
    derivation_path character varying(50),
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE auth_fullpubkey ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME auth_fullpubkey_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE btc_account_key (
    id bigint NOT NULL,
    coin coin_btc_bch NOT NULL,
    key_type character varying(20) DEFAULT 'bip44'::character varying NOT NULL,
    account account_type NOT NULL,
    p2pkh_address character varying(255) NOT NULL,
    p2sh_segwit_address character varying(255),
    bech32_address character varying(255),
    taproot_address character varying(255),
    full_public_key character varying(255) NOT NULL,
    multisig_address character varying(255) DEFAULT ''::character varying NOT NULL,
    redeem_script character varying(1000) DEFAULT ''::character varying NOT NULL,
    wallet_import_format character varying(255) NOT NULL,
    account_extended_privkey character varying(255),
    idx bigint NOT NULL,
    addr_status smallint DEFAULT 0 NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE btc_account_key ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME btc_account_key_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE eth_account_key (
    id bigint NOT NULL,
    account account_type NOT NULL,
    address character varying(42) NOT NULL,
    full_public_key character varying(130) NOT NULL,
    private_key character varying(64) NOT NULL,
    idx bigint NOT NULL,
    addr_status smallint DEFAULT 0 NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE eth_account_key ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME eth_account_key_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE musig2_nonces (
    id bigint NOT NULL,
    signer_id character varying(255) NOT NULL,
    transaction_id character varying(255) NOT NULL,
    public_nonce bytea NOT NULL,
    is_used boolean DEFAULT false NOT NULL,
    created_at timestamp with time zone DEFAULT now(),
    used_at timestamp with time zone
);

ALTER TABLE musig2_nonces ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME musig2_nonces_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE seed (
    id smallint NOT NULL,
    coin coin_all NOT NULL,
    seed character varying(255) NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE seed ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME seed_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_account_key (
    id bigint NOT NULL,
    coin coin_xrp NOT NULL,
    account account_type NOT NULL,
    account_id character varying(255) NOT NULL,
    key_type smallint DEFAULT 0 NOT NULL,
    master_key character varying(255) NOT NULL,
    master_seed character varying(255) NOT NULL,
    master_seed_hex character varying(255) NOT NULL,
    public_key character varying(255) NOT NULL,
    public_key_hex character varying(255) NOT NULL,
    is_regular_key_pair boolean DEFAULT false NOT NULL,
    allocated_id bigint DEFAULT 0 NOT NULL,
    addr_status smallint DEFAULT 0 NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE xrp_account_key ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_account_key_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_regular_key (
    id bigint NOT NULL,
    account_id character varying(255) NOT NULL,
    regular_key_address character varying(255) NOT NULL,
    public_key character varying(255) NOT NULL,
    public_key_hex character varying(255) NOT NULL,
    is_active boolean DEFAULT true NOT NULL,
    set_tx_hash character varying(255),
    created_at timestamp with time zone DEFAULT now(),
    rotated_at timestamp with time zone
);

ALTER TABLE xrp_regular_key ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_regular_key_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_signer_entry (
    id bigint NOT NULL,
    signer_list_id bigint NOT NULL,
    signer_account character varying(255) NOT NULL,
    signer_weight integer NOT NULL,
    created_at timestamp with time zone DEFAULT now()
);

ALTER TABLE xrp_signer_entry ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_signer_entry_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_signer_list (
    id bigint NOT NULL,
    account_id character varying(255) NOT NULL,
    signer_quorum integer NOT NULL,
    is_active boolean DEFAULT true NOT NULL,
    set_tx_hash character varying(255),
    created_at timestamp with time zone DEFAULT now(),
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE xrp_signer_list ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_signer_list_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY auth_fullpubkey
    ADD CONSTRAINT auth_fullpubkey_pkey PRIMARY KEY (id);

ALTER TABLE ONLY btc_account_key
    ADD CONSTRAINT btc_account_key_pkey PRIMARY KEY (id);

ALTER TABLE ONLY eth_account_key
    ADD CONSTRAINT eth_account_key_pkey PRIMARY KEY (id);

ALTER TABLE ONLY musig2_nonces
    ADD CONSTRAINT musig2_nonces_pkey PRIMARY KEY (id);

ALTER TABLE ONLY seed
    ADD CONSTRAINT seed_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_account_key
    ADD CONSTRAINT xrp_account_key_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_regular_key
    ADD CONSTRAINT xrp_regular_key_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_signer_entry
    ADD CONSTRAINT xrp_signer_entry_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_signer_list
    ADD CONSTRAINT xrp_signer_list_pkey PRIMARY KEY (id);

