-- Code generated by `make extract-sqlc-schema-postgres-watch`. DO NOT EDIT.
-- Extracted from dump_watch.postgres.sql

CREATE TABLE address (
    id bigint NOT NULL,
    coin coin_all NOT NULL,
    account account_type NOT NULL,
    wallet_address character varying(255) NOT NULL,
    is_allocated boolean DEFAULT false NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE address ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME address_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE btc_tx (
    id bigint NOT NULL,
    coin coin_btc_bch NOT NULL,
    action action_type NOT NULL,
    unsigned_hex_tx text NOT NULL,
    signed_hex_tx text NOT NULL,
    sent_hash_tx text NOT NULL,
    total_input_amount numeric(26,10) NOT NULL,
    total_output_amount numeric(26,10) NOT NULL,
    fee numeric(26,10) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    unsigned_updated_at timestamp with time zone DEFAULT now(),
    sent_updated_at timestamp with time zone
);

ALTER TABLE btc_tx ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME btc_tx_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE btc_tx_input (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    input_txid character varying(255) NOT NULL,
    input_vout integer NOT NULL,
    input_address character varying(255) NOT NULL,
    input_account character varying(255) NOT NULL,
    input_amount numeric(26,10) NOT NULL,
    input_confirmations bigint NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE btc_tx_input ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME btc_tx_input_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE btc_tx_output (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    output_address character varying(255) NOT NULL,
    output_account character varying(255) NOT NULL,
    output_amount numeric(26,10) NOT NULL,
    is_change boolean DEFAULT false NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE btc_tx_output ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME btc_tx_output_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE eth_detail_tx (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    uuid character varying(36) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    sender_account character varying(255) NOT NULL,
    sender_address character varying(255) NOT NULL,
    receiver_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount bigint NOT NULL,
    fee bigint NOT NULL,
    gas_limit integer NOT NULL,
    nonce bigint NOT NULL,
    unsigned_hex_tx text NOT NULL,
    signed_hex_tx text NOT NULL,
    sent_hash_tx text NOT NULL,
    unsigned_updated_at timestamp with time zone DEFAULT now(),
    sent_updated_at timestamp with time zone
);

ALTER TABLE eth_detail_tx ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME eth_detail_tx_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE payment_request (
    id bigint NOT NULL,
    coin coin_payment NOT NULL,
    payment_id bigint,
    sender_address character varying(255) NOT NULL,
    sender_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount numeric(26,10) NOT NULL,
    is_done boolean DEFAULT false NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE payment_request ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME payment_request_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE tx (
    id bigint NOT NULL,
    coin coin_eth_xrp_hyt NOT NULL,
    action action_type NOT NULL,
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE tx ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME tx_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_detail_tx (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    uuid character varying(36) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    sender_account character varying(255) NOT NULL,
    sender_address character varying(255) NOT NULL,
    receiver_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount character varying(255) NOT NULL,
    xrp_tx_type character varying(255) NOT NULL,
    fee character varying(255) NOT NULL,
    flags bigint NOT NULL,
    last_ledger_sequence bigint NOT NULL,
    sequence bigint NOT NULL,
    signing_pubkey character varying(255) NOT NULL,
    txn_signature character varying(255) NOT NULL,
    hash character varying(255) NOT NULL,
    earliest_ledger_version bigint NOT NULL,
    signed_tx_id character varying(255) NOT NULL,
    tx_blob text NOT NULL,
    sent_updated_at timestamp with time zone
);

ALTER TABLE xrp_detail_tx ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_detail_tx_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_multisig_signature (
    id bigint NOT NULL,
    pending_multisig_id bigint NOT NULL,
    signer_account character varying(255) NOT NULL,
    signed_tx_blob text NOT NULL,
    signer_weight integer NOT NULL,
    signed_at timestamp with time zone DEFAULT now()
);

ALTER TABLE xrp_multisig_signature ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_multisig_signature_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE xrp_pending_multisig (
    id bigint NOT NULL,
    tx_uuid character varying(36) NOT NULL,
    account_id character varying(255) NOT NULL,
    unsigned_tx_json text NOT NULL,
    xrp_tx_type character varying(50) NOT NULL,
    required_quorum integer NOT NULL,
    current_weight integer DEFAULT 0 NOT NULL,
    status multisig_status DEFAULT 'pending'::multisig_status NOT NULL,
    combined_tx_blob text,
    submitted_tx_hash character varying(255),
    expires_at timestamp with time zone,
    created_at timestamp with time zone DEFAULT now(),
    updated_at timestamp with time zone DEFAULT now()
);

ALTER TABLE xrp_pending_multisig ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME xrp_pending_multisig_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY address
    ADD CONSTRAINT address_pkey PRIMARY KEY (id);

ALTER TABLE ONLY btc_tx_input
    ADD CONSTRAINT btc_tx_input_pkey PRIMARY KEY (id);

ALTER TABLE ONLY btc_tx_output
    ADD CONSTRAINT btc_tx_output_pkey PRIMARY KEY (id);

ALTER TABLE ONLY btc_tx
    ADD CONSTRAINT btc_tx_pkey PRIMARY KEY (id);

ALTER TABLE ONLY eth_detail_tx
    ADD CONSTRAINT eth_detail_tx_pkey PRIMARY KEY (id);

ALTER TABLE ONLY payment_request
    ADD CONSTRAINT payment_request_pkey PRIMARY KEY (id);

ALTER TABLE ONLY tx
    ADD CONSTRAINT tx_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_detail_tx
    ADD CONSTRAINT xrp_detail_tx_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_multisig_signature
    ADD CONSTRAINT xrp_multisig_signature_pkey PRIMARY KEY (id);

ALTER TABLE ONLY xrp_pending_multisig
    ADD CONSTRAINT xrp_pending_multisig_pkey PRIMARY KEY (id);

