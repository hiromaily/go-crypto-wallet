-- Code generated by `make extract-sqlc-schema-postgresql-watch`. DO NOT EDIT.
-- Extracted from dump_watch.pg.sql

CREATE TABLE address (
    id bigint NOT NULL,
    coin text NOT NULL,
    account text NOT NULL,
    wallet_address character varying(255) NOT NULL,
    is_allocated boolean DEFAULT false NOT NULL,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT address_account_check CHECK ((account = ANY (ARRAY['client'::text, 'deposit'::text, 'payment'::text, 'stored'::text]))),
    CONSTRAINT address_coin_check CHECK ((coin = ANY (ARRAY['btc'::text, 'bch'::text, 'eth'::text, 'xrp'::text, 'hyt'::text])))
);

CREATE TABLE btc_tx (
    id bigint NOT NULL,
    coin text NOT NULL,
    action text NOT NULL,
    unsigned_hex_tx text NOT NULL,
    signed_hex_tx text NOT NULL,
    sent_hash_tx text NOT NULL,
    total_input_amount numeric(26,10) NOT NULL,
    total_output_amount numeric(26,10) NOT NULL,
    fee numeric(26,10) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    unsigned_updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    sent_updated_at timestamp without time zone,
    CONSTRAINT btc_tx_action_check CHECK ((action = ANY (ARRAY['deposit'::text, 'payment'::text, 'transfer'::text]))),
    CONSTRAINT btc_tx_coin_check CHECK ((coin = ANY (ARRAY['btc'::text, 'bch'::text])))
);

CREATE TABLE btc_tx_input (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    input_txid character varying(255) NOT NULL,
    input_vout integer NOT NULL,
    input_address character varying(255) NOT NULL,
    input_account character varying(255) NOT NULL,
    input_amount numeric(26,10) NOT NULL,
    input_confirmations bigint NOT NULL,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE btc_tx_output (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    output_address character varying(255) NOT NULL,
    output_account character varying(255) NOT NULL,
    output_amount numeric(26,10) NOT NULL,
    is_change boolean DEFAULT false NOT NULL,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE eth_detail_tx (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    uuid character varying(36) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    sender_account character varying(255) NOT NULL,
    sender_address character varying(255) NOT NULL,
    receiver_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount bigint NOT NULL,
    fee bigint NOT NULL,
    gas_limit integer NOT NULL,
    nonce bigint NOT NULL,
    unsigned_hex_tx text NOT NULL,
    signed_hex_tx text NOT NULL,
    sent_hash_tx text NOT NULL,
    unsigned_updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    sent_updated_at timestamp without time zone
);

CREATE TABLE payment_request (
    id bigint NOT NULL,
    coin text NOT NULL,
    payment_id bigint,
    sender_address character varying(255) NOT NULL,
    sender_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount numeric(26,10) NOT NULL,
    is_done boolean DEFAULT false NOT NULL,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT payment_request_coin_check CHECK ((coin = ANY (ARRAY['btc'::text, 'bch'::text, 'eth'::text, 'xrp'::text])))
);

CREATE TABLE tx (
    id bigint NOT NULL,
    coin text NOT NULL,
    action text NOT NULL,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT tx_action_check CHECK ((action = ANY (ARRAY['deposit'::text, 'payment'::text, 'transfer'::text]))),
    CONSTRAINT tx_coin_check CHECK ((coin = ANY (ARRAY['eth'::text, 'xrp'::text, 'hyt'::text])))
);

CREATE TABLE xrp_detail_tx (
    id bigint NOT NULL,
    tx_id bigint NOT NULL,
    uuid character varying(36) NOT NULL,
    current_tx_type smallint DEFAULT 1 NOT NULL,
    sender_account character varying(255) NOT NULL,
    sender_address character varying(255) NOT NULL,
    receiver_account character varying(255) NOT NULL,
    receiver_address character varying(255) NOT NULL,
    amount character varying(255) NOT NULL,
    xrp_tx_type character varying(255) NOT NULL,
    fee character varying(255) NOT NULL,
    flags bigint NOT NULL,
    last_ledger_sequence bigint NOT NULL,
    sequence bigint NOT NULL,
    signing_pubkey character varying(255) NOT NULL,
    txn_signature character varying(255) NOT NULL,
    hash character varying(255) NOT NULL,
    earliest_ledger_version bigint NOT NULL,
    signed_tx_id character varying(255) NOT NULL,
    tx_blob text NOT NULL,
    sent_updated_at timestamp without time zone
);

CREATE TABLE xrp_multisig_signature (
    id bigint NOT NULL,
    pending_multisig_id bigint NOT NULL,
    signer_account character varying(255) NOT NULL,
    signed_tx_blob text NOT NULL,
    signer_weight integer NOT NULL,
    signed_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE xrp_pending_multisig (
    id bigint NOT NULL,
    tx_uuid character varying(36) NOT NULL,
    account_id character varying(255) NOT NULL,
    unsigned_tx_json text NOT NULL,
    xrp_tx_type character varying(50) NOT NULL,
    required_quorum integer NOT NULL,
    current_weight integer DEFAULT 0 NOT NULL,
    status text DEFAULT 'pending'::text NOT NULL,
    combined_tx_blob text,
    submitted_tx_hash character varying(255),
    expires_at timestamp without time zone,
    created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT xrp_pending_multisig_status_check CHECK ((status = ANY (ARRAY['pending'::text, 'ready'::text, 'submitted'::text, 'confirmed'::text, 'failed'::text, 'expired'::text])))
);

