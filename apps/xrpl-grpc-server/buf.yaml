# buf.yaml - Buf configuration for xrpl-grpc-server TypeScript code generation
# https://buf.build/docs/configuration/v2/buf-yaml
version: v2

# Module name matching the root buf.yaml
name: buf.build/hiromaily/rippleapi

# Dependencies from Buf Schema Registry
deps:
  - buf.build/googleapis/googleapis
