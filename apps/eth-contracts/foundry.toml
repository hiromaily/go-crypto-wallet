[profile.default]
src = "contracts"
out = "out"
libs = ["lib", "node_modules"]

solc_version = "0.8.34"
optimizer = true
optimizer_runs = 200

[lint]
# False positive: transfer inside vm.expectRevert() always reverts, no return value to check
exclude_lints = ["erc20-unchecked-transfer"]
