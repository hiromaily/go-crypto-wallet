# Foundry build artifacts
out/
cache/

# Foundry broadcast artifacts (local deployment records — addresses change per Anvil run)
broadcast/

# Foundry libraries (installed via forge install)
lib/

# Node.js dependencies
node_modules/

# Environment variables (never commit private keys)
.env
